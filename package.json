{
  "name": "nuxt-vue-facing-decorator",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "HOST=0.0.0.0 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.10.2"
  },
  "dependencies": {
    "vue-facing-decorator": "^3.0.4"
  }
}
